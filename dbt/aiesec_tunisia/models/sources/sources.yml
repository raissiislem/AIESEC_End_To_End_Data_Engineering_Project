version: 2

sources:
  - name: bronze
    schema: bronze
    tables:
      - name: raw_performance
        description: "Raw AIESEC performance data scraped from core.aiesec.org, one row per scrape run."